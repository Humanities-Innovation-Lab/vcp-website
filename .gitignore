_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/

# Video is self-hosted. Keep individual files well under GitHub's 100 MB
# per-file hard limit, and use preload="none" so /viewer/ does not download
# them on page load.
